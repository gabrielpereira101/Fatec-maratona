#include <stdio.h>

int main() {
    int i, j;

    for(i = 1; i <= 9; i += 2) { // incrementa de 2 em 2
        for(j = 7; j >= 5; j--) { // decrementa de 1 em 1
            printf("I=%d J=%d\n", i, j);
        }
    }

    return 0;
}
